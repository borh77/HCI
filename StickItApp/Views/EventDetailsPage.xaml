<UserControl x:Class="StickItApp.Views.EventDetailsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Background="{DynamicResource ContentBrush}" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="12" HorizontalAlignment="Stretch">
            <TextBlock Text="{Binding Event.Name}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" />
            <TextBlock Text="{Binding Event.Id}" Foreground="{DynamicResource MutedTextBrush}" />

            <Border Height="110" Margin="0,10,0,0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                <Image Source="{Binding IconPreviewPath}" Stretch="Uniform" Margin="8" />
            </Border>

            <TextBlock Text="{Binding Event.Description}" Margin="0,12,0,0" TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}" />
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <TextBlock Text="{DynamicResource CityLabel}" Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text=": " Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text="{Binding Event.City}" Foreground="{DynamicResource TextBrush}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{DynamicResource CountryLabel}" Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text=": " Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text="{Binding Event.Country}" Foreground="{DynamicResource TextBrush}" />
            </StackPanel>
            <TextBlock Text="{Binding DateText}" Margin="0,10,0,0" Foreground="{DynamicResource TextBrush}" />
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{DynamicResource AverageCostLabel}" Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text=": " Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text="{Binding Event.AverageCost}" Foreground="{DynamicResource TextBrush}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{DynamicResource AttendanceLabel}" Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text=": " Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text="{Binding Event.Attendance}" Foreground="{DynamicResource TextBrush}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{DynamicResource CharitableLabel}" Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text=": " Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text="{Binding Event.IsCharitable}" Foreground="{DynamicResource TextBrush}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <TextBlock Text="{DynamicResource TypeLabel}" Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text=": " Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text="{Binding Type.Name}" Foreground="{DynamicResource TextBrush}" />
            </StackPanel>

            <TextBlock Text="{DynamicResource TagsLabel}" Margin="0,12,0,4" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" />
            <ItemsControl ItemsSource="{Binding Tags}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextBrush}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="{DynamicResource PreviousDatesLabel}" Margin="0,12,0,4" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" />
            <ItemsControl ItemsSource="{Binding PreviousDates}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Foreground="{DynamicResource TextBrush}">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0:yyyy-MM-dd} - {1:yyyy-MM-dd}">
                                    <Binding Path="Start" />
                                    <Binding Path="End" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Grid Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Height="38" Margin="0,0,4,0" ToolTip="{DynamicResource EditLabel}" Content="{DynamicResource EditLabel}" Command="{Binding EditCommand}" />
                <Button Grid.Column="1" Height="38" Margin="4,0,4,0" ToolTip="{DynamicResource DeleteLabel}" Content="{DynamicResource DeleteLabel}" Command="{Binding DeleteCommand}" />
                <Button Grid.Column="2" Height="38" Margin="4,0,0,0" Content="{DynamicResource BackLabel}" Command="{Binding BackCommand}" />
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
